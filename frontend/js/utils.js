const Utils={formatDate(d){if(!d)return'N/A';return new Date(d).toLocaleDateString('en-IN',{year:'numeric',month:'short',day:'numeric'})},formatDateTime(d){if(!d)return'N/A';return new Date(d).toLocaleDateString('en-IN',{year:'numeric',month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'})},getScoreColor(s){if(s>=70)return'green';if(s>=50)return'orange';return'red'},getStatusClass(s){var m={'APPLIED':'status-applied','SHORTLISTED':'status-shortlisted','REJECTED':'status-rejected','INTERVIEW_SCHEDULED':'status-interview','HIRED':'status-hired','OFFERED':'status-offered'};return m[s]||'status-applied'},getStatusLabel(s){return(s||'').replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase())},getGradeLabel(s){if(s>=90)return{grade:'A+',label:'Exceptional Match'};if(s>=80)return{grade:'A',label:'Strong Match'};if(s>=70)return{grade:'B+',label:'Good Match'};if(s>=60)return{grade:'B',label:'Moderate Match'};if(s>=50)return{grade:'C',label:'Partial Match'};if(s>=40)return{grade:'D',label:'Weak Match'};return{grade:'F',label:'Poor Match'}},truncate(t,m){m=m||100;if(!t||t.length<=m)return t||'';return t.substring(0,m)+'...'},getToken(){return localStorage.getItem('access_token')},saveTokens(a,r){localStorage.setItem('access_token',a);localStorage.setItem('refresh_token',r)},clearTokens(){localStorage.removeItem('access_token');localStorage.removeItem('refresh_token');localStorage.removeItem('user')},saveUser(u){localStorage.setItem('user',JSON.stringify(u))},getUser(){try{return JSON.parse(localStorage.getItem('user'))}catch(e){return null}},isLoggedIn(){return!!this.getToken()},requireAuth(){if(!this.isLoggedIn())window.location.href='../login.html'},getUserRole(){var u=this.getUser();return u?u.role:null}};
